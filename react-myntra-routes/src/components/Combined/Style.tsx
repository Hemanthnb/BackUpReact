import { Theme } from "@emotion/react";
import { SxProps } from "@mui/material";

export const style1:SxProps<Theme>={
    backgroundColor:"red"
}
export const style2:SxProps<Theme>={
    fontFamily: "Playwrite CO, cursive"
}
